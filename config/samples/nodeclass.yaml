apiVersion: karpenter.rackspace.com/v1
kind: RackspaceSpotNodeClass
metadata:
  name: default
spec:
  cloudspaceName: my-cloudspace
  labels:
    team: platform
  # Optional: narrow the eligible ServerClasses by category, region, etc.
  # serverClassSelector:
  #   matchLabels:
  #     category: gp
